project.manifest = manifest.xml

base.sourcePath = source
base.resourcePath = resources

# Default build uses the production server URL (ows.j0r1s.nl) so the PRG
# works when sideloaded to a watch. Dev builds (simulator with a local
# server) add dev.jungle to override this and use localhost.
base.excludeAnnotations = dev
